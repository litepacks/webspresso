/**
 * Migration template for email_logs table
 * @module plugins/email/migration-template
 */

/**
 * @param {string} [tableName='email_logs']
 * @returns {string}
 */
function generateEmailLogsMigration(tableName = 'email_logs') {
  return `/**
 * Migration: Create ${tableName} table
 * Auto-generated by email plugin
 */

exports.up = function(knex) {
  return knex.schema.createTable('${tableName}', (table) => {
    table.bigIncrements('id').primary();
    table.string('to', 1000).nullable();
    table.string('cc', 1000).nullable();
    table.string('bcc', 1000).nullable();
    table.string('subject', 500).nullable();
    table.string('template_id', 255).nullable().index();
    table.string('from', 255).nullable();
    table.string('status', 16).notNullable().index();
    table.string('message_id', 255).nullable();
    table.text('error').nullable();
    table.timestamp('created_at').defaultTo(knex.fn.now()).notNullable().index();
  });
};

exports.down = function(knex) {
  return knex.schema.dropTableIfExists('${tableName}');
};
`;
}

module.exports = {
  generateEmailLogsMigration,
};
